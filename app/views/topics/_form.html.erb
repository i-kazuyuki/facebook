<%= form_for(@topic) do |f| %>
  <%= f.error_messages! %>

  <i class="fa fa-pencil"></i><%= f.label :投稿を作成 %>
  
  <%= f.text_field :content, class: 'form-control' %>
  <br>
  <%= f.submit '投稿する', class: 'btn btn-primary' %>
<% end %>
