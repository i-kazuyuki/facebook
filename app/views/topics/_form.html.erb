<%= form_for(@topic) do |f| %>
  <%= f.error_messages! %>

  <%= f.label :content %>
  <%= f.text_field :content, class: 'form-control' %>
  <br>
  <%= f.submit '投稿する', class: 'btn btn-primary btn-block' %>
<% end %>
