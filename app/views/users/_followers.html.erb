<div class="container">
  <h3 class="left">フォロワー</h3>
  <% @user.followers.each do |user| %>
    <div class="container-child col-md-2 col-sm-4 col-xs-6 ">
      <p><%= profile_img(user)%></p>
      <p><%= user.name %></p>
      <p><%= render 'follow_form', user: user %></p>
    </div>
  <% end %>
</div>
